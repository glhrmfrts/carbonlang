extern(C) {

// Memory functions

func HeapAlloc(
  hHeap:   int,
  dwFlags: int,
  dwBytes: usize
): pointer;

func HeapFree(
  hHeap:   int,
  dwFlags: int,
  lpMem:   pointer
): bool;

func GetProcessHeap(): int;

// Command-line related functions

func GetCommandLineA(): *char;

}