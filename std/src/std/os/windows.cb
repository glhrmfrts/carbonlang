extern(C) {

// Memory functions

func HeapAlloc(
  hHeap:   int,
  dwFlags: int,
  dwBytes: usize
): raw_ptr;

func HeapFree(
  hHeap:   int,
  dwFlags: int,
  lpMem:   raw_ptr
): bool;

func GetProcessHeap(): int;

// Command-line related functions

func GetCommandLineA(): &char;

}