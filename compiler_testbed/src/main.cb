extern(C) func carbon_main() : int {
    let err = late_err()
    if (err != noerr) {
        
    }
    
    let fd,err = late_expression()

    let numbers,err = 0,noerr
}

func late_err() {
    return noerr
}

func late_expression() {
    return 2,noerr
}