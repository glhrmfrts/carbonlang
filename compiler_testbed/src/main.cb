func print(l: usize) {
}

extern(C) func carbon_main() : int {
    let err = late_err()
    if (err != noerr) {
        
    }
    
    let fd,err = late_expression()

    let numbers,err = 0,noerr

    let tup = 0,1,2,3,4
    print(tup.len)
}

func late_err() {
    return noerr
}

func late_expression() {
    return 2,noerr
}