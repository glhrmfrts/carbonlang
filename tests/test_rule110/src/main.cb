import io

const BOARD_CAP = 100

func main() {
    let board : [BOARD_CAP]int

    board[BOARD_CAP - 2] = 0

    for (i in 0, BOARD_CAP - 2) {
        for (j in 0, BOARD_CAP) {
            print(".*"[board[j]])
        }
        print("\n")
    }
}