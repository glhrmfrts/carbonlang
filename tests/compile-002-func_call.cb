func other_stuff(): int {
    return 2 + 3;
}

func foobar(a: int, b: int, c: int): int {
    let value = 50;
    let j = other_stuff();
    return value * j + a + b + c;
}

func f(x: int): int {
    return x*2;
}

func main(): int {
    //let val = 30;
    //let ok = foobar(val, foobar(1, foobar(4, 5, 6), 3), 0);

    let i = 3;
    return (i*7) + (50*f(1)) + (f(2)*70 + 23 + (f(3)*4));
}