import std::io;
import std::string;
import std::rawstring;

func square(x: int): int {
    return x*x;
}

func main(args: [*]String): int {
    println(args[0].data);
    let y = square(5).square().square();
    if (y == 5*5*25*625) {
        println("[OK] test_call_sugar");
    }
    else {
        println("[NOK] test_call_sugar");
    }
    return y;
}