// example vector math for parser test

type Vector2 = {x:float, y:float};

/*
func add(a: Vector2, b: Vector2): Vector2 {
	return a + b;
}

func sub(a: Vector2, b: Vector2): Vector2 {
	return a - b;
}

func mul(a: Vector2, b: Vector2) {
	return a * b;
}

func main(argc: int, argv: []raw_string): int {
	let vel: Vector2 = {.x=0, .y=0}; // TODO: init list
	let pos: Vector2 = {0,0};

	let state = State{
		.position = {0,0},
		.velocity = {0,0},
	};

	let arr = [2]float{1.0, 0.0};

	println(add(pos, vel));
	println(sub(pos, vel));
	println(mul(pos, vel));

	var ok: bool;
	return ok;
}

func other_stuff() {
	var a: int = 500;
	return 244 + a;
}

func main(): int {
	return other_stuff();
}
*/

// other_stuff:
// LOAD_LONG $1 500
// LOAD_LONG $2 412
// ADD_LONG_ABC $0 $1 $2
// RETURN
//
// main:
// CALL other_stuff
// RETURN