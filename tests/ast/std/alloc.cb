code_unit{
  std/alloc.cb
  decl_list{
    import_decl{ std::os::windows}
    func_decl{alloc (var_decl{size: type_expr{usize}}): type_expr{raw_ptr} compound_stmt{
        stmt_list{
          return_stmt{call{HeapAlloc (call{GetProcessHeap ()}, 0, size)}}}}}
    func_decl{free (var_decl{ptr: type_expr{raw_ptr}}) compound_stmt{
        stmt_list{
          call{HeapFree (call{GetProcessHeap ()}, 0, ptr)}}}}
    func_decl{copy (var_decl{dest: type_expr{raw_ptr}}, var_decl{src: type_expr{raw_ptr}}, var_decl{size: type_expr{usize}}) compound_stmt{
        decl_list{
          asm_stmt{push rdi
push rsi

mov rdi,rcx
mov rsi,rdx
mov rcx,r8
rep movsb

pop rsi
pop rdi
    }}}}
    func_decl{set (var_decl{dest: type_expr{raw_ptr}}, var_decl{value: type_expr{char}}, var_decl{count: type_expr{usize}}) compound_stmt{
        decl_list{
          asm_stmt{push rdi

mov rdi,rcx
mov rax,rdx
mov rcx,r8
rep stosb

pop rdi
    }}}}}}
