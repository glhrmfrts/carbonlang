code_unit{
  std/system.cb
  decl_list{
    type_decl{error = type_expr{int}}
    type_decl{memory = type_expr{struct_type{decl_list{
            var_decl{(ptr): type_expr{rawptr}}
            var_decl{(len): type_expr{usize}}}}}}
    linkage_specifier{ extern}
    linkage_specifier{ extern}
    linkage_specifier{ extern}
    linkage_specifier{ extern}
    linkage_specifier{ extern}
    var_decl{(O_CREATE) = 1}
    var_decl{(O_WRITE) = 2}
    var_decl{(O_READ) = 4}
    var_decl{(O_APPEND) = 8}
    linkage_specifier{ extern}
    linkage_specifier{ extern}
    linkage_specifier{ extern}
    linkage_specifier{ extern}
    linkage_specifier{ extern}
    linkage_specifier{ extern}
    var_decl{(SEEK_SET) = 0}
    var_decl{(SEEK_CUR) = 1}
    var_decl{(SEEK_END) = 2}
    var_decl{(SEEK_DATA) = 3}
    var_decl{(SEEK_HOLE) = 4}
    linkage_specifier{ extern}
    type_decl{stat_data = type_expr{struct_type{decl_list{
            var_decl{(size): type_expr{usize}}
            var_decl{(mtime): type_expr{int64}}
            var_decl{(atime): type_expr{int64}}
            var_decl{(ctime): type_expr{int64}}
            var_decl{(mode): type_expr{int}}}}}}
    linkage_specifier{ extern}
    linkage_specifier{ extern}
    linkage_specifier{ extern}
    linkage_specifier{ extern}
    linkage_specifier{ extern}
    linkage_specifier{ extern}
    linkage_specifier{ extern}
    linkage_specifier{ extern}}}
