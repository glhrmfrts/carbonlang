/*
    if let adap = ok(open_adapter()) {
        letfall conn = adap.open_connection(addr);
    }
    and ok(conn) {
        letfall value = conn.read_property(Property::ID);
    }
    and ok(value) {
        letfall parsed = value.parsed;
    }
    and ok(parsed) {
        letfall count = parsed.info.count;
    }
    else let read_err: ReadError {

    }
    else let conn_err: ConnectionError {

    }
*/