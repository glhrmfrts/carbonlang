import std::io
import std::string
import std::rawstring

type Array(T) = struct {
    data: &T
    len: usize
    cap: usize
}

func add(a: int, b: int) {
    return a + b
}

func main(args: []string) {
    println(args[0].data)
    let arr = Array(char){"beautiful", 10, 10}
    println(arr.data)
    return add(1, 2)
}