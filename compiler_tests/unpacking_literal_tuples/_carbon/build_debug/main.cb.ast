code_unit{
  main.cb
  decl_list{
    func_decl{get_values (var_decl{($cb_agg_ret): }):  {
        stmt_list{
          {{@ $cb_agg_ret} = init{
            {{@ $cb_agg_ret}.first = init{type=type_expr{} 
              {{@ $cb_agg_ret}.first.ptr = "Nice"}
              {{@ $cb_agg_ret}.first.len = cast_expr{ 4}}
              }}
            {{@ $cb_agg_ret}.second = 2}
            }}
          return{$cb_agg_ret}}}}
    linkage_specifier{ extern(C) func_decl{carbon_main (): type_expr{int} {
          decl_list{
            var_decl{(sz) = var_decl{($cbT0): type_expr{} = init{type=type_expr{} 
                  {$cbT0.ptr = "Hello"}
                  {$cbT0.len = cast_expr{ 5}}
                  }}
$cbT0.len}}
          stmt_list{
            return{cast_expr{type_expr{int} sz}}}}}}}}
