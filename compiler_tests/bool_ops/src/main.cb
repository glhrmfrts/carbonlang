import module as mod

extern(C) func carbon_main() : int {
    let sz : usize
    let ptr : &uint8
    
    let ok = ptr < sz

    let stat : mod::Stat

    let newptr = ptr + stat.st_ctime_nsec

    return 0
}