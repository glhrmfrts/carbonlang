func exit(code: int) {
asm%{
    mov $60, %rax           # system call 60 is exit
                            # code is already in rdi
    syscall
}%asm
}

extern(C) func carbon_main() {
    let code = "somestring".len

    // exit code should be 10
    exit(cast(int)code)
}