  code_unit{
    main.cb
    decl_list{
      func_decl{exit (var_decl{(code): type_expr{int}}) {
          decl_list{
            asm{    mov $60, %rax           # system call 60 is exit
                            # code is already in rdi
    syscall
}}}}
      linkage_specifier{ extern(C) func_decl{carbon_main () {
            decl_list{
              var_decl{(code) = var_decl{($cbT0): type_expr{} = init{type=type_expr{} 
                    {$cbT0.ptr = "somestring"}
                    {$cbT0.len = cast_expr{ 10}}
                    }}
$cbT0.len}}
            stmt_list{
              call{exit (cast_expr{type_expr{int} code})}}}}}}}
