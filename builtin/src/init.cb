/*import std::io
import std::string
import std::rawstring
import std::mem
import std::os::windows

private {

extern(C) func carbon_main(): int {
    let cmd_line_args_buf = [1]string{ string{"", 0} }

    let cmdline = nullcast(GetCommandLineA())

    // TODO: lengthof
    let args = []string{ &cmd_line_args_buf[0], 1 }
    args[0] = string{ cmdline, cmdline->strlen() }

    import main
    return main(args)
}

}
*/

extern(C) func carbon_main(): int {
    return 0
}